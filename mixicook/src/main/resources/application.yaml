spring:
  application:
    name: MixiCook
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/MixiCook}
    username: ${SPRING_DATASOURCE_USERNAME:kenato}
    password: ${SPRING_DATASOURCE_PASSWORD:kenato26052005}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${SERVER_PORT:8083}

app:
  jwt:
    secret: ${APP_JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    expiration-ms: ${APP_JWT_EXPIRATION:86400000}
  ml-service:
    url: ${ML_SERVICE_URL:http://localhost:8000/internal/predict}
